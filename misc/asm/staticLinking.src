// Calculer ch_STAT de la fonction appelée

LDQ %s, D0  // NX - NY + 1, D0 : un registre réservé

LDW A2, BP // A2 : un registre réservé

BOUCLE       LDW A2, (A2)-4 // -4 : déplacement statique
             ADQ -1, D0

BNE BOUCLE
