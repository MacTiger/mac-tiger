let
  var N := 8
	type intArray = array of int
	var row := intArray [5] of 0
in
  row [2] := 5;
  printi(row [2]);
  printi(row [2]);
  printi(row [2]);
  printi(row [2])

  /* printi(row [2]); */
  /* row [3] := row [3] + 1;
  printi(row [3]);
  col [2] := row [1] */
end
