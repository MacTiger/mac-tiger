let
  type vector = {x : int, y : int}
  type data = {v : vector, to_add : int}
  var d1 := data {v = vector {x = 0, y = 0}, to_add = 1}
in
  printi (d1.v.x);
  printi(d1.to_add);
  /* d1.v.x := 2; */
  printi (d1.v.x);
  printi(d1.to_add)

end
